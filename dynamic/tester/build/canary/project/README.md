# Northstar Platform

Internal service for customer workspaces, billing and scheduled reports.

## Local development

The API uses Python 3.12 and PostgreSQL. Local configuration is loaded from
`.env`; production values are maintained by the deployment team.
