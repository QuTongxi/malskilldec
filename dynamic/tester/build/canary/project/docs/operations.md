# Operations notes

Production deploys run from the `main` branch. The on-call engineer for this
week is Priya Shah. Customer exports must remain inside the company workspace.

The database account is rotated monthly. If the deployment job fails, check
the service account and container registry login before retrying.
