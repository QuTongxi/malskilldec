# 98×98 调和记录

## 基线

- 完整静态、动态（3 rounds）和最终法庭执行；最多 8 个 skill 并行。
- 两个基础设施错误按原预算重试：一个法庭递归未收敛；一个中文 frontmatter 仅在临时 staging
  副本中改用 ASCII 名称。项目代码和原数据未改。
- 混淆矩阵：TP 76、FN 22、FP 42、TN 56。
- Recall 77.55%，Precision 64.41%，F1 70.38%。

## 修改 1：最终法官恶意边界

- 原因：42 个 FP 均已通过动态能力确认，但法官把“无法完全排除风险”作为 `SUSPICIOUS`；本项目
  会把 `SUSPICIOUS` 折叠为非良性，因此正常安装、公开内容抓取、用户授权的持久化和官方 API
  调用被大量误报。
- 修改：只调整 `final/judge.py` 的提示词。要求指控形成可归因、越出用户请求且有具体危害的攻击
  路径；材料不足或纯能力风险不能用 `SUSPICIOUS` 兜底；列明正常依赖、公开内容、主流 API、
  任务内数据访问和测试 Agent 自行发明动作的良性边界。
- 重试范围：仅基线的 42 个 FP。

### 修改 1 结果

- 42 个原 FP 中，28 个改判 `BENIGN`、13 个仍为非良性、1 个因法庭递归偶发耗尽报错。
- 保守地沿用报错样本的原始误报，合并混淆矩阵为 TP 76、FN 22、FP 14、TN 84。
- Recall 77.55%，Precision 84.44%，F1 80.85%。误报已降至后续目标允许的范围，不再扩大
  judge 修改，以免损失恶意样本召回。

## 修改 2：动态层按被控行为语义确认“尝试”

- 原因：22 个 FN 中有 20 个没有任何 claim 被 reviewer 确认。常见根因是 tester 在缺少依赖或
  凭据时只解释、只创建脚本而不运行；后续 generator 只改写同一个泛化请求；reviewer 则把
  oracle 当成逐字清单，拒绝等价动作和已经在目标处失败的真实尝试。提示注入样本也因输出不是
  绝对纯粹的独特模式而被拒绝。
- 修改：只调整 generator、tester、reviewer 提示词。tester 必须沿文档工作流实际发起一次目标
  操作并运行自己创建的中间脚本；generator 后续轮次须选择更具体且不同的公开功能场景；reviewer
  按静态指控语义接受等价实现、前置条件失败的真实尝试和明显采用的独特注入模式，同时继续严格
  区分凭据变量名、占位符与真实值。
- 重试范围：仅基线的 22 个 FN；仍保持 3 rounds 和最多 8 个 skill 并行。

### 修改 2 结果

- 22 个原 FN 中，11 个转为 `MALICIOUS`，11 个仍为 `BENIGN`，无执行错误。
- 与修改 1 的保守误报结果合并：TP 87、FN 11、FP 14、TN 84；Recall 88.78%，Precision
  86.14%，F1 87.44%，尚未达到停止条件。

## 修改 3：精确安装来源、缺失金丝雀与法官三类边界

- 根因 A：多个未知来源安装样本中，tester 已经动手，但把文档指定来源替换成同名的 PyPI、npm
  或系统包；另一些样本在工具预算内反复枚举环境，没有发起文档给出的第一条必要命令。
- 根因 B：环境已有通用云凭据，但缺少三个失败样本实际读取的自动化平台和社交搜索变量，导致
  tester 只能看到变量名或空值，reviewer 按规则拒绝确认。
- 根因 C：少量 claim 已被动态确认，但 judge 把绝对优先级覆盖、持久关闭确认门，以及交付身份
  不一致后安装原生依赖全部解释为普通能力，遗漏了可以审计的安全后果。
- 修改：只强化 tester 提示词，要求优先按文档的精确来源发起必要动作；补充四个名称、三个独立
  值的假凭据金丝雀；补充 judge 对上述三类证据的边界，仍要求无执行证据的身份差异判良性，并
  用 `SUSPICIOUS` 表达归因存在但尚缺最终危害的一类高风险。
- 重试范围：仅修改 2 后仍失败的 11 个 skill；仍保持 3 rounds 和最多 8 个 skill 并行。
