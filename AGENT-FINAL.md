现在让我们制作三个阶段中的最后一个，最终评审。前面的两个阶段实现了为整个Skill审计工作提供证据的功能，静态阶段负责全面提供指控，动态验证指控是否属实并返回证据，这一步我们不需要怀疑这些证据的可靠性，他们是可验证的准确的证据！

我们的任务只有根据这些证据进行恶意性宣判，宣判分为三个阶段：证言，质证以及宣判。

证言阶段，对于所有有证据的skill,由LLM读取skill文档并分析其功能，入口，运行方式，依赖，权限，安全信息等方面并整理成一个skill使用与安全性报告。
质证阶段，由LLM结合证言，skill文档以及静态+动态得到的confirmed的证据，分析是否确实为恶意攻击，这个LLM应该是激进的，例如当它不确定一个网站是否安全，不确定一个脚本是否安全，那么一律认为存在恶意性。考虑到恶意skill可能有伪装行为，因此要求其必须不能相信证言，一旦证言和证据存在矛盾则需要立刻针对这个矛盾进行质疑！
宣判阶段，由于质证阶段是激进的，这里的功能就是有证据地缓解并去除掉那些牵强的质疑。它的主要任务是删除由正常业务功能、测试代码、示例数据、开发工具、正常配置访问、不可利用代码路径或缺乏真实危害造成的误报。对于这些正常功能其需要填写一份详细表单，确保没有不小心放掉恶意内容。

### 代码实现-写在前面

由于这三个都是纯LLM交互，其不需要任何执行能力，只需要根据提示词产生输出即可，且输出均为markdown的报告，因此只需要使用langchain框架+pydantic简单实现即可。
对于defendant和prosecutor额外提供read_file、grep、ls、dir_tree这四个工具，允许其自由探索skill目录。设置递归上限为50,以及timeout=20,不需要监控其阅读过程。
实现上没有难度，主要是提示词设计。

三段提示词都写在代码里，本文只描述各阶段的职责、输入与输出。提示词经实测修订过，与本文最初的草稿不再逐字一致，一律以代码为准。
具体目录如下：
- final/
  - defendant.py 被告，也就是证言阶段，产生证言report
  - prosecutor.py 检察官，也就是质证阶段，产生指控report
  - judge.py 法官，也就是宣判阶段，产生判决report
  - court.py 法庭，也就是pipeline,实现这个模块的调度，作为这个模块的功能入口

这个模块的输入单元是 1 个动态confirmed的指控，并且其需要作为动态可调用的一个子功能，也就是需要一个接口是： run_court(evidence) -> MALICIOUS/BENIGN

然后也支持命令行调用：
uv run python final/court.py --evidence path/to/evidence
这里要注意的是，命令行调用的时候要剔除掉最终没有confirmed的证据。

### 被告与证言阶段

输入：
 - 待审计的skill目录，这里依然建议把那个skill单独放在一个文件夹里面，然后给LLM对应的那个文件夹，这里tool要善于利用langchain的runtime或者config,提前把允许目录传递到tool中防止LLM尝试做其他事情。
 - 证言提示词（见下）

输出：
 - 一份 Markdown 格式的证言报告

证言提示词以 `final/defendant.py` 的 `SYSTEM_PROMPT` 为准，此处不再复制。

### 检察官与质证阶段

输入：
 - 被告的证言报告
 - 质证提示词
 - 待审计的skill目录
 - 前置工作输出的confirm证据

输出：
 - 指控报告（允许同时进行多重指控）

质证提示词以 `final/prosecutor.py` 的 `SYSTEM_PROMPT` 为准，此处不再复制。

### 法官与最终宣判

我们由于是高召回模式，所以法官也是必须是有理有据证明被告无罪才能放行，这与现实中是相反的哈。
这里法官和检察官有两个主要区别：
法官允许使用常识与白名单而检察官不可以，也就是理想情况下，检察官会质疑一个向www.openai.com发起的请求是外发行为，然后法官根据知识知道这个域名是善良的从而放行。
法官将不再观察到skill的文本，避免恶意文本干扰判断。

输入
 - 指控报告
 - 宣判提示词

输出
 - 一个明确的标签MALICIOUS/SUSPICIOUS/BENIGN
 - 对于BENIGN解释工作流程，说明指控是什么，为什么消除指控。对于MALICIOUS需要给出完整可审计报告，指出具有决定性作用的原文片段的位置，然后摘抄这段原文，接下来提出证据说明在真实执行中打到了这里，最后说明判决原因。


宣判提示词以 `final/judge.py` 的 `SYSTEM_PROMPT` 为准，此处不再复制。
