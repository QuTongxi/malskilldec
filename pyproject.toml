[project]
name = "malskilldet"
version = "0.1.0"
description = "Static + dynamic detection of malicious agent skills"
requires-python = ">=3.12"
dependencies = [
    "deepagents",
    "langchain",
    "langchain-openai",
    "pydantic",
    "python-dotenv",
    "pyyaml",
]
