我需要你重新撰写整个恶意skill检测工具，这一次我们要确保代码结构简单，之前我发现我那个太复杂冗长，根本没办法改。

## 静态分析
对于静态分析部分，依旧是正则匹配+codeql

### 设计
正则匹配条目来自于https://github.com/Tencent/AI-Infra-Guard，https://github.com/getsentry/skills/tree/main/skills/skill-scanner等一系列github公开skill扫描器的正则串，正则器的架构为：

（regex, level）其中level=critical,high.medium,low四个分级，表示这个串有多大可能性是恶意内容串，我们用分级代替之前的不容易解释的自然数评分。

正则分组：
由于某些正则器在语义上有相似的语义，我们要进行分组，这个组不是针对恶意类型，而是针对正则器行为。比如网络访问，外发，凭据读取，授权，执行等等，这里可以分组详细一点。

一个正则匹配的finding包含：
- metadata：元数据，用来放一些基本信息的，比如id,skill路径等等，以后你想要加一些过程中用来查找/定位的参数都加到这里，finding来源
- matched_text：匹配到的原文
- context：上下文，不低于300字符
- location：（文件名，行号，列号）
- level：严重程度
- group：正则分组的组别

CodeQL发现：
codeql部分依然是扫描ts/js,python,然后将其输出整理成finding，这里context就记录codeql的数据流就好了，然后我们自己去指定level以及group.

聚合：
最终要聚合为8种恶意类型，请阅读 @report.pdf 来具体了解，文件里面是9种，但是我们不考虑那个金钱交易相关的，所以8种。这个聚合就是按照组来定义的，一个组可以同时服务于多个恶意类型，一个恶意类型可以有多个组。

评分：这一次我们根据findings的危险等级，以及当不同类型findings聚合的时候，可能会产生新的语义，例如当凭据操作与外发同时存在的时候，就可以额外增加score,这里加分我们只分为high和low.然后就是分数转化，依然是数量无关的，也就是同一个组内，无论匹配1条还是100条，都是只看level最高的，组间则是我们的语义加分控制，然后就是各个level对应多少分数了，你可以自己决定。

最终聚合后的结构是指控，其包含：
- metadata：元数据，从findings那里继承的
- level：根据 @report.pdf 进行的分级
- score：危险分数，根据其包含的findings计算出来
- type：8种类型之一
- findings：[] 具体的findings列表

### 文件结构

- static/
  - codeql/ codeql工作目录，放codeql,数据库，启动脚本
  - matchers.py 存放正则匹配的串，存放一些具体配置，放评分逻辑
  - workers.py 实现单独的正则匹配，codeql的执行逻辑
  - pipeline.py 实现工作流程，处理输入输出

### 输入输出契约

输入为一个文件夹，需要递归搜索内容，当看到SKILL.md或者skill.md的时候停止并认定当前目录是一个需扫描的skill。
命令行简化输出，命令行输出每个skill包含有多少指控，指控类型以及指控level即可。
文件输出是一个json,包含所有skill的所有指控

### 运行方式

项目采用uv运行：
uv run python static/pipeline.py 
可选参数： --disable-codeql 表示不启用codeql,默认是启动的（不用codeql的时候就是正则匹配扫描所有文件）

### 代码规范与准则

禁止撰写无意义代码，无意义错误处理，无意义逻辑，复杂无用字段，莫名其妙try-except或者莫名其妙if.用户一定按照契约处理输出，我们是科研项目不是工程！

代码要简洁但是不偷工减料，按照要求实现，注重可读性，也就是让人一眼能看懂而不是工程上要求的那种封装，严密！

禁止先给我设计v1或者先设计小框架，实验版！直接按照要求设计，比如这里正则匹配的串应该是上百条的，不能是你先写1个！codeql也是，那个数据库也是要建立完善的，不可以是写一点点！

### 测试

逻辑测试：
禁止撰写pytest测试代码，没有必要！测试用的数据集已经打包成zip了，你需要解压然后使用malicious/这个目录做测试。在测试的时候先--disable-codeql，这样运行应该是秒级的。确保没出现意料之外问题之后打开codeql对所有测试数据集再次扫描，这一次应该会慢一点因为codeql会稍微慢一些，能跑通即可。

功能测试：
这个时候检查所有的输出，确保malicious/中的skill均有输出即可，对于benign/会有什么输出我们根本不关心！然后对于输出指控较少（3条之内）的skill做单独检查，确保输出的是其恶意内容所在的段落。




